import { ProductProductPipe } from './product/product.pipe';

describe('ProductProductPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductProductPipe();
    expect(pipe).toBeTruthy();
  });
});
